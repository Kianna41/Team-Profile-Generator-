const Manager = require("../lib/manager");

describe("Manager", () => {
  describe("getNumber", () => {
    it("should return the office phone number");
    expect(Manger.getNumber()).toEqual(123);
  });
});
